interface:
  display_name: "Polydev"
  short_description: "Run parallel agent worktrees"
  default_prompt: "Use the polydev skill to run these independent implementation tasks in parallel worktrees."
