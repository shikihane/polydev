# Worktree Executor

Use when running parallel implementation work in Git worktrees.

## Essentials

- Launch worktree sessions with the Polydev scripts.
- Keep one workspace name per project.
- Monitor with `poll.sh`.
- Use `task.toon` as the source of truth for status.
- Close the session before removing the worktree.

